# Notes files

Place public note files in this directory, for example:

- `linear-algebra.pdf`
- `probability.pdf`
- `machine-learning.md`

Then add a `link` field to the matching entry in `content/notes.toml`:

```toml
link = "/todd-zhu-portfolio/notes/linear-algebra.pdf"
```
